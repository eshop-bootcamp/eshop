spring:
  datasource:
    url: jdbc:h2:file:~/test;IFEXISTS=TRUE;AUTO_SERVER=TRUE;AUTO_SERVER_PORT=9092;INIT=SET SCHEMA eshop;
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none